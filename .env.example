# Copy to .env and adjust before deploy:
#   cp .env.example .env

# Host ports (prefer 2222/2223 for class; avoid binding 22 without approval)
COWRIE_SSH_HOST_PORT=2222
COWRIE_TELNET_HOST_PORT=2223

# Fake hostname shown inside the honeypot shell
COWRIE_HOSTNAME=lab-srv01
